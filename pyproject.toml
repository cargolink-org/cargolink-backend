[project]
name = "cargolink-backend"
version = "0.1.0"
description = "CargoLink — Smart Freight Matching and Tracking System backend (FastAPI)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
