"""Dependency-injection container — NOT wired to any repository yet.

Task A.2 is schema/routing only, per its explicit instructions ("Touch
app/core/di.py registrations" is listed under DO NOT). Repository
interfaces, mock implementations, and the DI wiring that resolves them
(mock vs. real, via Settings.mock_repo) are Task A.3's responsibility.

This file is treated as pre-existing per A.1 and is left untouched/empty
by A.2 on purpose.
"""

# Intentionally empty pending Task A.3.
